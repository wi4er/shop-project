import { Meta, StoryObj } from '@storybook/angular';
import { MainFeaturesComponent } from './main-features.component';

const meta: Meta<MainFeaturesComponent> = {
  title: 'Components/MainFeatures',
  component: MainFeaturesComponent,
};

export default meta;
type Story = StoryObj<MainFeaturesComponent>;

export const DesktopBig: Story = {
};
